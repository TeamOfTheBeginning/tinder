package com.first.tinder.dao;

import com.first.tinder.entity.OpponentMemberInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OpponentMemberInfoRepository extends JpaRepository<OpponentMemberInfo, Integer> {
}
